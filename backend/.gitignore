# Python virtual environment
.venv/

# Python cache
__pycache__/
*.pyc

# Secrets
.env
.env.*

# One-off database dumps from the Supabase project migration (125 MB each). Kept locally
# as a rollback; must never reach the repo — GitHub rejects files over 100 MB anyway.
musicx_*.sql
migration_baseline.txt

# The record of what the archive job removed, kept locally for reference.
archive_deleted_*.txt

# The dev API log written by run.sh.
.dev-api.log
